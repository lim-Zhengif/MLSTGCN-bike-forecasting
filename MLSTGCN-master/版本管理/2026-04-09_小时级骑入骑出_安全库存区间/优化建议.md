#
别盲目加 epoch，改成“max_epoch=50~60 + early stopping + ReduceLROnPlateau”，这样更稳。
比起加轮次，更值得先调 lr 和学习率调度。你现在 lr=1e-4，可以试 5e-5 或 2e-4。
如果你的目标是“峰时风险”和“安全库存区间”更准，那比起纯 MAE，更该考虑给早晚高峰或库存边界加权，不然模型容易学到平均值，但边界覆盖率不够强。